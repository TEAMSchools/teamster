[project]
name = "teamster-mcp"
version = "0.1.0"
description = "MCP servers for teamster"
requires-python = ">=3.13,<3.15"
dependencies = ["mcp>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dagster_plus"]
