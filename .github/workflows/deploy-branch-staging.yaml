name: staging

on:
  workflow_dispatch: null
  pull_request:
    branches:
      - main
    paths:
      - src/teamster/**
      - .github/workflows/deploy-branch-staging.yaml
    types:
      - opened
      - synchronize
      - reopened
      - closed

jobs:
  dagster-cloud-deploy:
    uses: TEAMSchools/teamster/.github/workflows/dagster-cloud-deploy.yaml@main
    with:
      code-location: staging
