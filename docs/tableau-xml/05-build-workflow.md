# The build workflow

Moved. The step order is "The loop" in
[`.claude/skills/tableau-workbook-xml/SKILL.md`](../../.claude/skills/tableau-workbook-xml/SKILL.md);
the code behind each step is
[`references/build-workflow.md`](../../.claude/skills/tableau-workbook-xml/references/build-workflow.md).
The publish gate there runs before the publish call, not after it. This stub is
kept so existing links resolve; edit the skill, not this file.
