execution:
  config:
    multiprocess:
      max_concurrent: 80
ops:
  compose_resyncs:
    config:
      step_size: 125000
  execute_query_4:
    ops:
      get_data:
        config:
          query_timeout: 120
  compose_tables:
    config:
      tables:
        - name: "assignmentscore"
          projection: "assignmentscoreid,yearid,assignmentsectionid,studentsdcid,islate,iscollected,isexempt,ismissing,isabsent,isincomplete,actualscoreentered,actualscorekind,actualscoregradescaledcid,scorepercent,scorepoints,scorelettergrade,scorenumericgrade,scoreentrydate,scoregradescaledcid,altnumericgrade,altalphagrade,altscoregradescaledcid,hasretake,authoredbyuc"
          queries:
            - q:
                selector: "assignmentscoreid"
                value: "resync"
