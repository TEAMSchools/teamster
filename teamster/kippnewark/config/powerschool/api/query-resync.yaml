ops:
  compose_tables:
    config:
      tables:
        - name: "calendar_day"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "cc"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "log"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "storedgrades"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "pgfinalgrades"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "attendance"
          queries:
            - q:
                selector: "dcid"
                value: "resync"
        - name: "assignmentsection"
          projection: "assignmentsectionid,yearid,sectionsdcid,assignmentid,relatedgradescaleitemdcid,name,duedate,description,scoretype,scoreentrypoints,extracreditpoints,weight,totalpointvalue,iscountedinfinalgrade,isscoringneeded,publishoption,publishdaysbeforedue,publishonspecificdate,publisheddate,publishedscoretypeid,isscorespublish,maxretakeallowed"
          queries:
            - q:
                selector: "assignmentsectionid"
                value: "resync"
        - name: "assignmentcategoryassoc"
          projection: "assignmentcategoryassocid,assignmentsectionid,teachercategoryid,yearid,isprimary"
          queries:
            - q:
                selector: "assignmentcategoryassocid"
                value: "resync"
