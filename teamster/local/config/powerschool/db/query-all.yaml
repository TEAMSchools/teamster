execution:
  config:
    resources:
      limits:
        cpu: 750m
        memory: 1.0Gi
ops:
  attendance:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: attendance
        output_fmt: file
        partition_size: 100000
  log:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: log
        output_fmt: file
        partition_size: 100000
  assignmentscore:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: assignmentscore
        output_fmt: file
        partition_size: 100000
  pgfinalgrades:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: pgfinalgrades
        output_fmt: file
        partition_size: 100000
  storedgrades:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: storedgrades
        output_fmt: file
        partition_size: 100000
