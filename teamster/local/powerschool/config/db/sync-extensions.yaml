ops:
  config:
    queries:
      - sql:
          schema:
            table:
              name: s_nj_crs_x
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: s_nj_ren_x
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: s_nj_stu_x
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: s_nj_usr_x
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: u_clg_et_stu
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: u_clg_et_stu_alt
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: u_def_ext_students
            where:
              column: whenmodified
      - sql:
          schema:
            table:
              name: u_studentsuserfields
            where:
              column: whenmodified
