execution:
  config:
    resources:
      limits:
        cpu: 250m
        memory: 0.5Gi
resources:
  sftp:
    config:
      remote_host:
        env: PYTHONANYWHERE_SFTP_HOST
      username:
        env: PYTHONANYWHERE_SFTP_USERNAME
      password:
        env: PYTHONANYWHERE_SFTP_PASSWORD
ops:
  config:
    destination:
      name: alchemer
      type: sftp
      path: ./sftp/alchemer
    query:
      sql:
        schema:
          table:
            name: surveygizmo_contacts
            schema: gabby.extracts
    file:
      stem: alchemer_contacts
      suffix: json
