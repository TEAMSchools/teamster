execution:
  config:
    multiprocess:
      max_concurrent: 2
resources:
  db:
    config:
      prefetch_rows: 100000
ops:
  compose_queries:
    config:
      destination:
        name: powerschool
        type: gcs
      queries:
        - sql:
            schema:
              table: students
        - sql:
            schema:
              columns: dcid
              table: storedgrades
