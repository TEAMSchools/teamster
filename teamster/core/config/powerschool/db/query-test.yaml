ops:
  bell_schedule:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: bell_schedule
        output_fmt: file
  calendar_day:
    config:
      destination:
        type: file
      query:
        sql:
          schema:
            table:
              name: calendar_day
        output_fmt: file
