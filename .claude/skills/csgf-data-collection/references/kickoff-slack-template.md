# CSGF Kickoff Slack Message Template

This is the actual message sent to the CSGF-dedicated Slack channel for the
2026-2027 cycle (Step 6 of the main skill procedure), kept here as a **template
for next cycle** rather than a one-off. Reuse the structure and wording; swap
out everything marked `[ADJUST]`. If a section stops applying (e.g. a feature
CSGF added this cycle isn't new next cycle), drop it rather than forcing it to
stay.

---

📊 CSGF Data Collection `[ADJUST: cycle years]` — kickoff (Slack edition this
year!)

Hi team (@channel) — no in-person kickoff meeting this cycle. Here's everything
you need, plus CSGF's optional office hours if you want live help.

**The 3 most important deadlines to know today:**

- `[ADJUST: pre-work date]` — your Salesforce Portal access is ready. That means
  you've checked (or taken whatever steps were needed) to get an account, and
  it's actually working, no later than `[date]`.
- `[ADJUST: internal deadline date]` — our internal deadline, and it's
  **non-negotiable** this cycle because of a change in how CSGF defines "done."
  (Only claim "non-negotiable because X changed" if something actually did --
  don't carry this framing forward by default; state the real reason for the
  current cycle instead if it's different.)
- `[ADJUST: official deadline date]` — CSGF has **accepted** all of our Round 1
  data. Not just submitted — accepted.

**What changed since last year:**

- `[ADJUST: list this cycle's actual portal/process changes]` -- pull these from
  the item-list doc rollover (Step 3) and the Portal mechanics reference; don't
  invent generic-sounding changes.
- `[Anything else specific to your item — see below]`

📄 **Read this first — this is your prework:**
`[link to this cycle's item-list doc]`. Set aside 30-45 minutes to read it and
confirm your Salesforce Portal account actually works. If it doesn't, email
**datacollection@chartergrowthfund.org** to get one now — don't wait until
closer to `[pre-work date]`.

📅 **Outlook invites you'll get today:**

- `[ADJUST: list this cycle's actual invites/dates -- see the item-list doc's Context section for the authoritative list]`

👤 **Specific to you:**

- `[ADJUST per cycle -- name the actual item owners and their actual deadlines/gotchas. Don't leave a prior cycle's names/specifics in place unchanged -- confirm ownership against the current item-list doc first (Step 3's "what tends to change" checklist covers exactly this).]`

Questions → this channel. 🙌

---

## Notes for reuse

- The 2026-2027 message left one per-person item as an open placeholder
  (`[HSDC Round 2 — Verified Matriculation + NSC file — due "early 2027," exact date TBD from CSGF]`)
  rather than guessing a firm date. Do the same when a real date isn't known yet
  -- don't fabricate specificity.
- Draft this message the same session as Step 6 (resolving open questions), so
  per-person specifics reflect resolved ownership, not draft-stage open
  questions from the item-list doc.
- This message is Slack-formatted (bold via `*`, not markdown `**`, when
  actually posting) -- the version above uses standard markdown for readability
  in this reference file; convert formatting when copying into Slack.
