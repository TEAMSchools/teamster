---
paths:
  - "**/*gradebook_audit*"
---

# Gradebook audit models

Invoke the `gradebook-audit` skill before changing this file. It carries the
dashboard's lineage, scaffold rules, and the checks a change must pass.
