dagsterCloud:
  branchDeployments: true

dagsterCloudAgent:
  resources:
    limits:
      cpu: "250m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
    requests:
      cpu: "250m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"

workspace:
  serverTTL:
    # fullDeployments:
    #   enabled: true
    #   ttlSeconds: 1800 # 30 min
    branchDeployments:
      ttlSeconds: 1800 # 30 min
  resources:
    limits:
      # cpu: "250m"
      # memory: "512Mi"
      ephemeral-storage: "10Gi"
    requests:
      # cpu: "250m"
      # memory: "512Mi"
      ephemeral-storage: "10Gi"
