locations:
  - location_name: kippnewark
    code_source:
      python_file: teamster/local/repository.py
    build:
      directory: .
      registry: us-central1-docker.pkg.dev/teamster-332318/teamster-kippnewark/teamster-kippnewark
    working_directory: /root/app
    container_context:
      k8s:
        env_secrets:
          - kippnewark
        # volumes:
        #   - name: secret-volume
        #     secret:
        #       secretName: kippnewark-ssh-keys
        # volume_mounts:
        #   - name: secret-volume
        #     readOnly: true
        #     mountPath: "/etc/secret-volume"
