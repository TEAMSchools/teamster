endpoints:
  - asset_name: behavior
    api_version: beta
    params:
      sdt: "{fiscal_year_start}"
      edt: "{fiscal_year_end}"
      UpdatedSince: "{partition_key}"
      IncludeDeleted: Y
  - asset_name: homework
    api_version: beta
    params:
      sdt: "{fiscal_year_start}"
      edt: "{fiscal_year_end}"
      UpdatedSince: "{partition_key}"
      IncludeDeleted: Y
  - asset_name: comm
    api_version: beta
    params:
      UpdatedSince: "{partition_key}"
      IncludeDeleted: Y
      IncludePrevEnrollments: Y
  - asset_name: incidents
    api_version: v1
    params:
      StartDate: "{fiscal_year_start}"
      EndDate: "{fiscal_year_end}"
      UpdatedSince: "{partition_key}"
      IncludeDeleted: Y
      cf: Y
  - asset_name: followups
    api_version: v1
    params:
      UpdatedSince: "{partition_key}"
