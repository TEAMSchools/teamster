assets:
  - dataset_config:
      dataset_id: kipptaf_extracts
      table_id: rpt_egencia__users
    file_config:
      stem: users_{today}
      suffix: csv
    destination_config:
      name: egencia
      path: /global/50323/USERS
