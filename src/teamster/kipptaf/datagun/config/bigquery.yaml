assets:
  - dataset_config:
      dataset_id: extracts
      table_id: rpt_idauto__staff_roster
    extract_job_config:
      destination_format: CSV
    file_config:
      stem: AD
      suffix: csv
    destination_config:
      name: idauto
