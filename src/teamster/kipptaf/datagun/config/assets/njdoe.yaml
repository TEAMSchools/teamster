assets:
  - asset_name: bgcheck_audit_json
    key_prefix:
      - kipptaf
      - datagun
      - njdoe
    query_config:
      type: schema
      value:
        table:
          name: njdoe_bgcheck_audit
          schema: extracts
        where: (is_approved = 0 OR is_approved_district = 0)
    file_config:
      stem: bgcheck_audit
      suffix: json
    destination_config:
      name: pythonanywhere
      path: sftp/njdoe
