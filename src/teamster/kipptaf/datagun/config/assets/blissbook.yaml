assets:
  - asset_name: members_csv
    key_prefix:
      - datagun
      - blissbook
    query_config:
      type: schema
      value:
        table:
          name: blissbook_employee_list
          schema: extracts
    file_config:
      stem: members
      suffix: csv
    destination_config:
      name: blissbook
