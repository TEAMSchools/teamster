key_prefix:
  - kippcamden
  - datagun
  - cpn
assets:
  - asset_name: cpn_attendance_csv
    query_config:
      type: schema
      value:
        table:
          name: cpn_attendance
          schema: extracts
        where: "[School Year] = gabby.utilities.GLOBAL_ACADEMIC_YEAR()"
    file_config:
      stem: cpn_attendance
      suffix: csv
    destination_config:
      name: cpn
      path: /Data
    op_tags:
      dagster-k8s/config:
        container_config:
          resources:
            limits:
              memory: 1.0Gi
  - asset_name: cpn_demographic_csv
    query_config:
      type: schema
      value:
        table:
          name: cpn_demographic
          schema: extracts
        where: "[School Year] = gabby.utilities.GLOBAL_ACADEMIC_YEAR()"
    file_config:
      stem: cpn_demographic
      suffix: csv
    destination_config:
      name: cpn
      path: /Data
