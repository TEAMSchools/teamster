assets:
  - asset_name: kipp_newark_student_data_txt
    key_prefix:
      - kippnewark
      - datagun
      - nps
    query_config:
      type: schema
      value:
        table:
          name: nps_student_info
          schema: extracts
    file_config:
      stem: kipp-newark-student-data
      suffix: txt
    destination_config:
      name: nps
      path: sftp
