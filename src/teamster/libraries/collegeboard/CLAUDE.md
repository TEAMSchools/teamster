# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

## Purpose

Avro schemas for **College Board** assessment data (SAT, AP, PSAT) ingested via
SFTP. The asset is built using `sftp.build_sftp_file_asset()` in the code
location.
