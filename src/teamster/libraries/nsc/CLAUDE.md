# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

## Purpose

Avro schemas for **National Student Clearinghouse (NSC)** college enrollment
verification data ingested via SFTP. The asset is built using
`sftp.build_sftp_file_asset()` in the code location.
