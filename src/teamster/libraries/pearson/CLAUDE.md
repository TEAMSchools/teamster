# CLAUDE.md — `teamster/libraries/pearson/`

Avro schemas for **Pearson** state assessment data (PARCC, NJGPA) ingested via
SFTP. Used by NJ district code locations. The asset is built using
`sftp.build_sftp_file_asset()` in the code location.
