sources:
  - name: edplan
    schema:
      "{{ resolve_source_schema(var('edplan_schema', project_name ~ '_edplan'))
      }}"
    tables:
      - name: stg_edplan__njsmart_powerschool_archive
      - name: src_edplan__njsmart_powerschool_archive
        config:
          enabled: false
