version: 2

sources:
  - name: alchemer
    schema: "{{ project_name }}_alchemer"
    tables:
      - name: base_alchemer__survey_results
        config:
          enabled: true
        meta:
          dagster:
            asset_key:
              - kipptaf
              - alchemer
              - base_alchemer__survey_results
