sources:
  - name: kippmiami_focus
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      elif target.name == 'staging' -%}zz_stg_{%- endif -%}kippmiami_focus"
    tables:
      - name: stg_focus__schools
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - stg_focus__schools
      - name: stg_focus__students
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - stg_focus__students
      - name: stg_focus__student_enrollment
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - stg_focus__student_enrollment
      - name: stg_focus__attendance_calendars
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - stg_focus__attendance_calendars
      - name: stg_focus__attendance_calendar
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - stg_focus__attendance_calendar
      - name: int_focus__school_year_first_day
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__school_year_first_day
      - name: int_focus__student_enrollment
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__student_enrollment
      - name: int_focus__course_periods__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__course_periods__pivot
      - name: int_focus__courses__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__courses__pivot
      - name: int_focus__master_courses__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__master_courses__pivot
      - name: int_focus__schools__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__schools__pivot
      - name: int_focus__student_enrollment__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__student_enrollment__pivot
      - name: int_focus__students__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__students__pivot
      - name: int_focus__users__pivot
        config:
          meta:
            dagster:
              group: focus
              asset_key:
                - kippmiami
                - focus
                - int_focus__users__pivot
