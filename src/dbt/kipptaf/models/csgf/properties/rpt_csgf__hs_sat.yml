version: 2
models:
  - name: rpt_csgf__hs_sat
    config:
      contract:
        enforced: true
    columns:
      - name: studentid
        data_type: int64
      - name: section
        data_type: string
      - name: score
        data_type: numeric
