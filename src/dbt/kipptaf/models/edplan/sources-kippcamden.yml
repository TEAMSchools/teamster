sources:
  - name: kippcamden_edplan
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      elif target.name == 'staging' -%}zz_stg_{%- endif -%}kippcamden_edplan"
    tables:
      - name: int_edplan__njsmart_powerschool_union
        config:
          meta:
            dagster:
              group: edplan
              asset_key:
                - kippcamden
                - edplan
                - int_edplan__njsmart_powerschool_union
