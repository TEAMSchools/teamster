version: 2
models:
  - name: stg_finance__payroll_code_mapping
    config:
      contract:
        enforced: true
    columns:
      - name: old_project_id_alt_nj
        data_type: int64
      - name: project_id
        data_type: int64
      - name: project_name
        data_type: string
