models:
  - name: stg_google_sheets__performance_management__leadership_development_metrics
    description: >
      Archive only, frozen 2026-07-30. Leadership Development moved to Lattice.
      The frozen table holds only metrics that were enabled at freeze time,
      because this model filtered `where not disabled`; the full metric list
      stays in the source Google Sheet. Registered as
      google_sheets.stg_google_sheets__performance_management__leadership_development_metrics_archive.
    # Refs #4627
    config:
      enabled: false
    columns:
      - name: Academic_Year
        data_type: int64
      - name: Region
        data_type: string
      - name: Metric_ID
        data_type: string
      - name: Bucket
        data_type: string
      - name: Role
        data_type: string
      - name: Type
        data_type: string
      - name: Description
        data_type: string
      - name: Disabled
        data_type: boolean
      - name: fiscal_year
        data_type: int64
