sources:
  - name: kipppaterson_titan
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      endif -%}kipppaterson_titan"
    tables:
      - name: stg_titan__person_data
        config:
          meta:
            dagster:
              group: titan
              asset_key:
                - kipppaterson
                - titan
                - stg_titan__person_data
      - name: stg_titan__income_form_data
        config:
          enabled: false
          meta:
            dagster:
              group: titan
              asset_key:
                - kipppaterson
                - titan
                - stg_titan__income_form_data
