sources:
  - name: kippcamden_titan
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      endif -%}kippcamden_titan"
    tables:
      - name: stg_titan__person_data
        config:
          meta:
            dagster:
              group: titan
              asset_key:
                - kippcamden
                - titan
                - stg_titan__person_data
