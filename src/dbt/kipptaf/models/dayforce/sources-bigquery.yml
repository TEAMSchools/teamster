sources:
  - name: dayforce
    schema: |
      {% if target.name == 'dev' -%}zz_dev_{{ env_var('GITHUB_USER', '') }}_
      {%- elif target.name == 'staging' or env_var('DBT_CLOUD_ENVIRONMENT_TYPE', '') == 'staging' -%}zz_staging_
      {%- endif -%}kipptaf_dayforce
    tables:
      - name: int_dayforce__employee_history
        config:
          meta:
            dagster:
              asset_key:
                - kipptaf
                - dayforce
                - int_dayforce__employee_history
