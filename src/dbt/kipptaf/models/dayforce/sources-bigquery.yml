sources:
  - name: dayforce
    schema: "{{ project_name }}_dayforce"
    tables:
      - name: int_dayforce__employee_history
        config:
          meta:
            dagster:
              asset_key:
                - kipptaf
                - dayforce
                - int_dayforce__employee_history
