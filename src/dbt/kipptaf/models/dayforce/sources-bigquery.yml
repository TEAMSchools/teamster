sources:
  - name: dayforce
    schema: "{{ resolve_source_schema(project_name ~ '_dayforce') }}"
    tables:
      - name: int_dayforce__employee_history
        config:
          meta:
            dagster:
              asset_key:
                - kipptaf
                - dayforce
                - int_dayforce__employee_history
