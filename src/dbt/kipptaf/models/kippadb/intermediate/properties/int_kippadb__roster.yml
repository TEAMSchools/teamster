models:
  - name: int_kippadb__roster
    columns:
      - name: student_number
        data_type: int64
        data_tests:
          - unique:
              config:
                store_failures: true
