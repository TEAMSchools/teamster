exposures:
  - name: dbt_snapshots
    label: dbt Snapshots
    type: analysis
    owner:
      name: Data Team
    depends_on:
      - ref("snapshot_leadership_development__output")
      - ref("snapshot_people__employee_numbers")
      - ref("snapshot_people__student_logins")
    config:
      meta:
        dagster:
          kinds:
            - dbt
            - table
  - name: dbt_semantic_layer
    label: dbt Semantic Layer
    type: ml
    owner:
      name: Data Team
    depends_on:
      - ref("dim_dates")
      - ref("dim_schools")
      - ref("dim_seats")
      - ref("dim_staff")
      - ref("dim_students")
      - ref("dim_teammates")
      - ref("dim_terms")
      - ref("fct_attendance")
      - ref("fct_microgoals")
    config:
      meta:
        dagster:
          kinds:
            - dbt
            - semanticmodel
  - name: ad_hoc_reporting_views
    label: Ad Hoc Reporting
    type: analysis
    owner:
      name: Data Team
    depends_on:
      - ref("int_powerschool__attendance_streak")
    config:
      meta:
        dagster:
          kinds:
            - dbt
            - view
