models:
  - name: dim_student_assessment_expectations
    description: >-
      Student assessment expectation dimension. One row per student x internal
      assessment x administration window. Represents which assessments each
      student is expected to take based on their grade level and enrollment,
      scaffolded from Illuminate assessment metadata and student session
      affiliation. Only covers internal (Illuminate) assessments.
    columns:
      - name: student_assessment_expectation_key
        data_type: string
        description: >-
          Surrogate key.
        data_tests:
          - unique
          - not_null

      - name: assessment_key
        data_type: string
        description: >-
          Stores a title name for the teacher and is used for sorting purposes.
          Surrogate key.

      - name: term_key
        data_type: string
        description: >-
          The code value that is stored in the database, such as Home and Work
          for phone types and Mand F for gender types. Indexed with the Code
          Type column. Required Surrogate key.

      - name: student_number
        data_type: int64
        description: >-
          District-assigned student identification number.

        config:
          meta:
            source_system: PowerSchool
            source_model: stg_powerschool__student_email
            source_column: student_number
            contains_pii: true
      - name: assessment_id
        data_type: int64
        description: >-
          Illuminate assessment ID.

      - name: academic_year
        data_type: int64
        description: >-
          Starting calendar year of the school year, extracted from school_year
          (e.g., 2025 from '2025-2026').

        config:
          meta:
            source_system: Amplify
            source_model: stg_amplify__mclass__sftp__pm_student_summary_aimline
            source_column: academic_year
      - name: administered_at
        data_type: date
        description: >-
          Scheduled administration date for the assessment.

      - name: assessment_title
        data_type: string
        description: >-
          Stores a title name for the teacher and is used for sorting purposes.

        config:
          meta:
            source_system: PowerSchool
            source_model: stg_powerschool__users
            source_column: title
      - name: subject_area
        data_type: string
        description: >-
          Subject area of the assessment.

      - name: scope
        data_type: string
        description: >-
          Assessment scope (e.g., CMA, CGI).

      - name: module_type
        data_type: string
        description: >-
          Module type (e.g., QA, CR).

      - name: module_code
        data_type: string
        description: >-
          Module code identifying the assessment variant.

      - name: grade_level_id
        data_type: int64
        description: >-
          Grade level ID the assessment targets.

      - name: powerschool_school_id
        data_type: int64
        description: >-
          PowerSchool school ID where the student is enrolled.

      - name: region
        data_type: string
        description: >-
          Network region (Newark, Camden, Miami, Paterson).

      - name: is_internal_assessment
        data_type: boolean
        description: >-
          Always TRUE for this model, which only includes internal Illuminate
          assessments.
