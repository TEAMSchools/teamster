select
    powerschool_student_number as student_number,
    academic_year,
    scope,
    subject_area,
    discipline,
    module_type,
    module_code,
    region,
    powerschool_school_id,
    is_internal_assessment,
    is_replacement,
    response_type,
    response_type_id,
    response_type_code,
    response_type_description,
    response_type_root_description,
    date_taken,
    points,
    percent_correct,
    title,
    assessment_id,
    administered_at,
    performance_band_set_id,
    n_assessments,
    is_multipart_assessment,
    assessment_ids,
    performance_band_label,
    performance_band_label_number,
    is_mastery,
    term_administered,
    term_taken,
    {{
        dbt_utils.generate_surrogate_key(
            [
                "powerschool_student_number",
                "assessment_id",
                "TO_JSON_STRING(assessment_ids)",
                "response_type",
                "response_type_id",
                "response_type_code",
            ]
        )
    }} as internal_assessments_key,
from {{ ref("int_assessments__response_rollup") }}
