models:
  - name: fct_student_course_enrollments

    columns:
      - name: student_course_enrollments_key
        data_type: string
        tests:
          - unique:
              config:
                severity: Warn
                store_failures: true

      - name: _dbt_source_relation
        data_type: string

      - name: academic_year
        data_type: int64

      - name: student_number
        data_type: int64

      - name: discipline
        data_type: string

      - name: teachernumber
        data_type: string

      - name: teacher_name
        data_type: string

      - name: course_number
        data_type: string

      - name: course_name
        data_type: string

      - name: school
        data_type: string

      - name: student_tier
        data_type: string

      - name: is_tutoring
        data_type: boolean

      - name: iready_proficiency_eoy
        data_type: string
