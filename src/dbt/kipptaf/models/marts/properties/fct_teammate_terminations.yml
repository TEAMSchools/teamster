models:
  - name: fct_teammate_terminations

    columns:
      - name: employee_number
        data_type: int64

      - name: academic_year
        data_type: int64

      - name: termination_status
        data_type: string

      - name: termination_reason
        data_type: string

      - name: termination_effective_date
        data_type: date

      - name: is_termination
        data_type: boolean
