version: 2

sources:
  - name: students
    schema: |
      {% if env_var('DBT_CLOUD_ENVIRONMENT_TYPE', '') == 'dev' -%}z_dev_{%- endif -%}
      {{ project_name }}_students
    tables:
      - name: src_students__graduation_path_combos
        external:
          options:
            format: GOOGLE_SHEETS
