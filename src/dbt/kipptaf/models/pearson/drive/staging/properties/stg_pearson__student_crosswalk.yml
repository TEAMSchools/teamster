version: 2
models:
  - name: stg_pearson__student_crosswalk
    config:
      contract:
        enforced: true
      tags: google_sheet
    columns:
      - name: Student_Test_UUID
        data_type: string
      - name: Student_Number
        data_type: int64
