sources:
  - name: kipppaterson_pearson
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      elif target.name == 'staging' -%}zz_stg_{%- endif -%}kipppaterson_pearson"
    tables:
      - name: int_pearson__njsla
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - int_pearson__njsla
      - name: int_pearson__njsla_science
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - int_pearson__njsla_science
      - name: int_pearson__student_list_report
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - int_pearson__student_list_report
      - name: int_pearson__all_assessments
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - int_pearson__all_assessments
