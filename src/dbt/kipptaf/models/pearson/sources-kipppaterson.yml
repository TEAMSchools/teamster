sources:
  - name: kipppaterson_pearson
    schema: |
      {% if target.name == 'dev' -%}zz_dev_{{ env_var('GITHUB_USER', '') }}_
      {%- elif target.name == 'staging' or env_var('DBT_CLOUD_ENVIRONMENT_TYPE', '') == 'staging' -%}zz_staging_
      {%- endif -%}kipppaterson_pearson
    tables:
      - name: stg_pearson__njgpa
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__njgpa
      - name: stg_pearson__njsla
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__njsla
      - name: stg_pearson__njsla_science
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__njsla_science
      - name: stg_pearson__parcc
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__parcc
      - name: stg_pearson__student_list_report
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__student_list_report
      - name: stg_pearson__student_test_update
        config:
          meta:
            dagster:
              group: pearson
              asset_key:
                - kipppaterson
                - pearson
                - stg_pearson__student_test_update
