version: 2
models:
  - name: stg_assessments__student_number_xwalk
    config:
      contract:
        enforced: true
      tags: google_sheet
    columns:
      - name: Student_Test_UID
        data_type: string
      - name: Student_Number
        data_type: int64
