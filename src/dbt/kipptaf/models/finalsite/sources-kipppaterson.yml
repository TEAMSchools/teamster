sources:
  - name: kipppaterson_finalsite
    schema: "{{ resolve_region_source_schema('kipppaterson_finalsite') }}"
    tables:
      - name: stg_finalsite__status_report
        config:
          meta:
            dagster:
              group: amplify
              asset_key:
                - kipppaterson
                - finalsite
                - stg_finalsite__status_report
