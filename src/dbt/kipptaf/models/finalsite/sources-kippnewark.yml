sources:
  - name: kippnewark_finalsite
    schema: "{{ resolve_region_source_schema('kippnewark_finalsite') }}"
    tables:
      - name: stg_finalsite__status_report
        config:
          meta:
            dagster:
              group: amplify
              asset_key:
                - kippnewark
                - finalsite
                - stg_finalsite__status_report
