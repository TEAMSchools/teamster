sources:
  - name: kippmiami_finalsite
    schema: "{{ resolve_region_source_schema('kippmiami_finalsite') }}"
    tables:
      - name: stg_finalsite__status_report
        config:
          meta:
            dagster:
              group: amplify
              asset_key:
                - kippmiami
                - finalsite
                - stg_finalsite__status_report
