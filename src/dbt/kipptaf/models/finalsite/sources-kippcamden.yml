sources:
  - name: kippcamden_finalsite
    schema: "{{ resolve_region_source_schema('kippcamden_finalsite') }}"
    tables:
      - name: stg_finalsite__status_report
        config:
          meta:
            dagster:
              group: amplify
              asset_key:
                - kippcamden
                - finalsite
                - stg_finalsite__status_report
