sources:
  - name: kippcamden_finalsite
    schema: |
      {%- if env_var('DBT_CLOUD_ENVIRONMENT_TYPE', '') in ['dev', 'staging'] -%}z_dev_
      {%- endif -%}kippcamden_finalsite
    tables:
      - name: stg_finalsite__status_report
        meta:
          dagster:
            group: amplify
            asset_key:
              - kippcamden
              - finalsite
              - stg_finalsite__status_report
