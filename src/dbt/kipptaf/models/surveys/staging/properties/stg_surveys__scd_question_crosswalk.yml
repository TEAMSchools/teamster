version: 2
models:
  - name: stg_surveys__scd_question_crosswalk
    config:
      contract:
        enforced: true
      tags: google_sheet
    columns:
      - name: question_code
        data_type: string
      - name: question_text
        data_type: string
