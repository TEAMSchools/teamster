models:
  - name: int_surveys__kfwd_career_launch_reconciliation
    config:
      materialized: table
    columns:
      - name: survey_response_id
        data_type: string
        data_tests:
          - unique:
              config:
                severity: warn
                store_failures: true
      - name: sf_contact_id
        data_type: string
