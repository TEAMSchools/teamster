version: 2
models:
  - name: stg_crdc__student_numbers
    config:
      contract:
        enforced: true
    columns:
      - name: crdc_question_section
        data_type: string
      - name: student_number
        data_type: numeric
