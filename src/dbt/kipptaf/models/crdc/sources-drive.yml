version: 2

sources:
  - name: crdc
    schema: |
      {% if env_var('DBT_DEV', '') == 'true' -%}z_dev_kipptaf_crdc
      {%- else -%}kipptaf_crdc
      {%- endif %}
    tables:
      - name: src_crdc__sced_code_crosswalk
        external:
          options:
            format: GOOGLE_SHEETS
            uris:
              - https://docs.google.com/spreadsheets/d/1pDwUdnK3Kcqjk_Bwp6-8Wz9Kr55AmEC7rA-EGuU6jGA
            sheet_range: src_crdc__sced_code_crosswalk
            skip_leading_rows: 1
        columns:
          - name: sced_code
            data_type: string
          - name: crdc_subject
            data_type: string
          - name: crdc_course_name
            data_type: string
        meta:
          dagster:
            asset_key:
              - kipptaf
              - crdc
              - sced_code_crosswalk
