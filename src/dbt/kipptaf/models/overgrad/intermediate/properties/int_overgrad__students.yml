models:
  - name: int_overgrad__students
    config:
      materialized: table
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - _dbt_source_relation
              - id
          config:
            severity: error
    columns:
      - name: _dbt_source_project
        data_type: string
