sources:
  - name: kippnj_renlearn
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      endif -%}kippnewark_renlearn"
    tables:
      - name: stg_renlearn__accelerated_reader
        config:
          meta:
            dagster:
              group: renlearn
              asset_key:
                - kippnewark
                - renlearn
                - stg_renlearn__accelerated_reader
      - name: stg_renlearn__star
        config:
          meta:
            dagster:
              group: renlearn
              asset_key:
                - kippnewark
                - renlearn
                - stg_renlearn__star
