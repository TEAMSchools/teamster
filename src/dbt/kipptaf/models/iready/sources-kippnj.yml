sources:
  - name: kippnj_iready
    schema:
      "{%- if target.name == 'dev' -%}zz_{{ env_var('GITHUB_USER', 'dev') }}_{%-
      endif -%}kippnewark_iready"
    tables:
      - name: stg_iready__diagnostic_results
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - stg_iready__diagnostic_results
      - name: stg_iready__instructional_usage_data
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - stg_iready__instructional_usage_data
      - name: stg_iready__instruction_by_lesson
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - stg_iready__instruction_by_lesson
      - name: stg_iready__instruction_by_lesson_pro
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - stg_iready__instruction_by_lesson_pro
      - name: stg_iready__personalized_instruction_summary
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - stg_iready__personalized_instruction_summary
      - name: int_iready__personalized_instruction_unpivot
        config:
          meta:
            dagster:
              group: iready
              asset_key:
                - kippnewark
                - iready
                - int_iready__personalized_instruction_unpivot
