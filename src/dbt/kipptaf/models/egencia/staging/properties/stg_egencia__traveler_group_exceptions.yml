version: 2
models:
  - name: stg_egencia__traveler_group_exceptions
    config:
      contract:
        enforced: true
    columns:
      - name: employee_number
        data_type: int64
      - name: traveler_group
        data_type: string
