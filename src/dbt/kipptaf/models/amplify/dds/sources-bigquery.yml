sources:
  - name: amplify
    schema:
      "{%- if target.name in ['defer', 'dev'] -%}zz_{{ env_var('GITHUB_USER',
      'dev') }}_{%- elif target.name == 'staging' -%}zz_stg_{%- endif -%}{{
      project_name }}_amplify"
    tables:
      - name: int_amplify__dds__data_farming_unpivot
        identifier: int_amplify__dibels_data_farming_unpivot
        config:
          meta:
            dagster:
              asset_key:
                - kipptaf
                - amplify
                - dds
                - int_amplify__dibels_data_farming_unpivot
