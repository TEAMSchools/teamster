models:
  - name: base_powerschool__sections
    description: >-
      Compatibility passthrough over int_students__course_sections, which holds
      the section logic and the column documentation. Scheduled for removal once
      its remaining consumers migrate, tracked in #3999. Carries no tests of its
      own — they run on the model underneath, and repeating them here would
      rescan every column for no additional coverage.
