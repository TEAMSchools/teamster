models:
  - name: base_powerschool__course_enrollments
    description: >-
      Compatibility passthrough over `int_students__course_enrollments`, which
      holds the course enrollment logic and the column documentation. Scheduled
      for removal once its remaining consumers migrate, tracked in #3999.
      Carries no tests of its own — they run on the model underneath, and
      repeating them here would rescan every column for no additional coverage.
