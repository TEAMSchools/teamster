version: 2

models:
  - name: base_people__staff_roster
    columns:
      - name: employee_number
        tests:
          - unique
          - not_null
    config:
      meta:
        dagster:
          freshness_policy:
            maximum_lag_minutes: 1440
            cron_schedule: 0 0 * * *
            cron_schedule_timezone: America/New_York
