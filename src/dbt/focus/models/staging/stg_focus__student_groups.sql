select
    id,
    title,
    district_id,
    saved_search_id,
    assignment_type,
    assignment_values,
    schools,
    visible_to_profiles,
    limit_visible_profiles,
    limit_assigned_users,
    default_search_for_assigned,
    requires_eligibility,
    parent_portal,
    student_portal,
    start_date,
    end_date,
    last_refresh,
    uuid,
    created_at,
    updated_at,
from {{ source("focus", "student_groups") }}
