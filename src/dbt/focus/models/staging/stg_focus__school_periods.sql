select
    period_id,
    rollover_id,
    school_id,
    district_id,
    syear,
    title,
    short_name,
    block,
    attendance,
    required_for_scheduling,
    conflicts_with,
    sort_order,
    length,
    start_time,
    end_time,
    uuid,
    created_at,
    updated_at,
from {{ source("focus", "school_periods") }}
