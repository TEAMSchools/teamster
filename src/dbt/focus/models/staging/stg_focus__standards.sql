select
    id,
    syear,
    school_id,
    district_id,
    title,
    short_name,
    description,
    sort_order,
    category_1_id,
    category_2_id,
    category_3_id,
    category_4_id,
    rollover_id,
    marking_periods,
    hide_on_report_card,
    cte_import,
    cpalms,
    grade_scale_title,
    guid,
    implementation_year,
    obsolete_year,
    adopt_year,
    publication,
    created_at,
    updated_at,
from {{ source("focus", "standards") }}
