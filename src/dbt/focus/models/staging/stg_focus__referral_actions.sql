select
    id,
    title,
    local_code,
    state_code,
    severity,
    profiles_edit,
    min_days_referral,
    max_days_referral,
    max_days_year,
    max_days_ese,
    max_days_504,
    warning_level,
    min_grade_level,
    max_grade_level,
    withdraw_code,
    restrict_reentry,
    school_exemptions,
    max_referral_days_ese,
    max_referral_days_504,
    override_profile,
    warning_message,
    start_year,
    end_year,
    detention,
    district_id,
    letter_id,
    letter_source,
    grade_levels,
    type,
    created_at,
    updated_at,
from {{ source("focus", "referral_actions") }}
