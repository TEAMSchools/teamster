select
    id,
    district_id,
    title,
    short_name,
    data_type,
    summary_type,
    select_options,
    transcript,
    sort_order,
    min_score,
    max_score,
    min_score1,
    max_score1,
    min_score2,
    max_score2,
    min_score3,
    max_score3,
    min_score4,
    max_score4,
    created_at,
    updated_at,
from {{ source("focus", "test_history_score_types") }}
