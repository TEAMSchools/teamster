select
    id,
    syear,
    school_id,
    title,
    sort_order,
    gpa_value,
    default_breakoff,
    scale_id,
    credits,
    weighted_gpa_value,
    description,
    is_incomplete,
    exclude_grades,
    uuid,
    gpa_averaging_cutoff,
    gpa_averaging_points,
    created_at,
    updated_at,
from {{ source("focus", "report_card_grades") }}
