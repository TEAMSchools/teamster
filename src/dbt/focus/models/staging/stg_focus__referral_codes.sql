select
    id,
    title,
    code,
    state_value,
    sesir,
    max_iss,
    min_iss,
    max_oss,
    min_oss,
    actions,
    profiles,
    restrict_reentry,
    severity,
    override_profile,
    warning_message,
    end_year,
    incident_type,
    start_year,
    mobile_app,
    district_id,
    letter_id,
    require_hope,
    require_threat,
    letter_source,
    grade_levels,
    probation_option_ids,
    priority,
    created_at,
    updated_at,
from {{ source("focus", "referral_codes") }}
