select
    id,
    test_id,
    district_id,
    fas_section_id,
    title,
    short_name,
    subject_code,
    subject,
    sort_order,
    score_types,
    transcript,
    min_scale_score,
    min_score,
    max_syear,
    graph_score,
    uuid,
    created_at,
    updated_at,
from {{ source("focus", "test_history_parts") }}
