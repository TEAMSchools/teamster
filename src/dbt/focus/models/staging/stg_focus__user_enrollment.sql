select
    id,
    staff_id,
    profiles,
    schools,
    districts,
    schools_array,
    profiles_array,
    erp_profiles,
    one_access_request_id,
    comment,
    imported,
    imported1,
    start_date,
    end_date,
    uuid,
    created_at,
    updated_at,
from {{ source("focus", "user_enrollment") }}
