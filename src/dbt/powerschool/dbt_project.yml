name: powerschool
version: 1.0.0
config-version: 2
require-dbt-version: [">=1.3.0", <2.0.0]

profile: powerschool

model-paths: [models]
analysis-paths: [analyses]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

target-path: target
clean-targets:
  - target
  - dbt_packages

vars:
  current_academic_year: 0
  local_timezone: UTC
  bigquery_external_connection_name: null
  powerschool_external_source_type: odbc

models:
  +schema: powerschool
