models:
  - name: stg_powerschool__gradplan
    config:
      enabled: false
      contract:
        enforced: false
    columns:
      - name: name
        data_type: string
      - name: id
        data_type: int64
        data_tests:
          - unique:
              config:
                store_failures: true
      - name: plantype
        data_type: int64
