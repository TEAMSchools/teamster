models:
  - name: stg_powerschool__assignmentcategoryassoc
    columns:
      - name: assignmentcategoryassocid
        data_type: int64
        data_tests:
          - unique:
              config:
                severity: error
        description:
          Primary key. Unique sequential number generated by the application.
          Required. Indexed.
        config:
          meta:
            contains_pii: false
      - name: whocreated
        data_type: string
        description:
          The user login that created the record. Defaults to database user.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whencreated
        data_type: timestamp
        description:
          Date and time the record was created. Defaults to system date.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whomodified
        data_type: string
        description:
          The user login that modified the record. Defaults to database user.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whenmodified
        data_type: timestamp
        description:
          Date and time the record was modified. Defaults to system date.
          Required.
        config:
          meta:
            contains_pii: false
      - name: ip_address
        data_type: string
      - name: whomodifiedtype
        data_type: string
      - name: transaction_date
        data_type: timestamp
      - name: executionid
        data_type: string
      - name: _dagster_partition_fiscal_year
        data_type: int64
      - name: _dagster_partition_date
        data_type: date
      - name: _dagster_partition_hour
        data_type: int64
      - name: _dagster_partition_minute
        data_type: int64
      - name: assignmentsectionid
        data_type: int64
        description:
          Foreign key. Internal number for the associated
          AssignmentSection.AssignmentSectionIDrecord. Required. Indexed.
        config:
          meta:
            contains_pii: false
      - name: teachercategoryid
        data_type: int64
        description:
          Foreign key. Internal number for the associated
          TeacherCategory.TeacherCategoryID record.Required. Indexed.
        config:
          meta:
            contains_pii: false
      - name: yearid
        data_type: int64
        description:
          A number representing which year the term belongs to, such as 13 for
          2003-2004. The numberis equal to the ID of the year term divided by
          100. Maintained in the Terms table.
        config:
          meta:
            contains_pii: false
      - name: isprimary
        data_type: int64
        description:
          "Indicates whether this category a primary category. Valid values:
          1=Yes, 0=No. Required."
        config:
          meta:
            contains_pii: false
      - name: whomodifiedid
        data_type: int64
    description:
      This table allows the association of multiple categories to an assignment.
