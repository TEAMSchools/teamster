version: 2
models:
  - name: stg_powerschool__emailaddress
    config:
      contract:
        enforced: true
    #      pre_hook:
    #        CALL BQ.REFRESH_EXTERNAL_METADATA_CACHE\('\{\{ source\("powerschool",
    #        "\w+"\) \| replace\("`", ""\) \}\}'\)
    columns:
      - name: emailaddress
        data_type: string
      - name: whocreated
        data_type: string
      - name: whencreated
        data_type: timestamp
      - name: whomodified
        data_type: string
      - name: whenmodified
        data_type: timestamp
      - name: emailaddressid
        data_type: int64
