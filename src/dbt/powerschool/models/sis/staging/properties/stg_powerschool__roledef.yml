models:
  - name: stg_powerschool__roledef
    columns:
      - name: name
        data_type: string
        description:
          User configurable name uniquely identifying the role to the user
          within a RoleModule Required.
        config:
          meta:
            contains_pii: false
      - name: rolekey
        data_type: string
        description:
          Internal code uniquely identifying a role within a RoleModule.
          Required.
        config:
          meta:
            contains_pii: false
      - name: description
        data_type: string
        description: User configurable description.
        config:
          meta:
            contains_pii: false
      - name: whocreated
        data_type: string
      - name: whencreated
        data_type: timestamp
        description:
          The date and time the record was created. Defaults to system date.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whomodified
        data_type: string
        description:
          The name of person who last modified the record. Defaults to database
          user. Required.
        config:
          meta:
            contains_pii: false
      - name: whenmodified
        data_type: timestamp
        description:
          The date and time the record was last modified. Defaults to system
          date. Required.
        config:
          meta:
            contains_pii: false
      - name: id
        data_type: int64
        data_tests:
          - unique:
              config:
                severity: error
        description:
          Unique sequential number generated by the application. Required.
        config:
          meta:
            contains_pii: false
      - name: rolemoduleid
        data_type: int64
        description:
          Internal number for the associated RoleModule record. Indexed.
          Required.
        config:
          meta:
            contains_pii: false
      - name: islocked
        data_type: int64
        description:
          1 indicates the user can delete this role. 0 indicates the user cannot
          delete this role. Defaults to 0.Required.
        config:
          meta:
            contains_pii: false
      - name: isvisible
        data_type: int64
        description:
          1 indicates this role appears in the Role Administration page.
          Defaults to 1. Required.
        config:
          meta:
            contains_pii: false
      - name: isenabled
        data_type: int64
        description:
          1 indicates this role should continue to be usable in the application
          for new associations. Defaults to 1.Required.
        config:
          meta:
            contains_pii: false
      - name: sortorder
        data_type: int64
        description:
          Determines the order in which this item appears in the list of roles
          from lowest to highest numberDefaults to 0. Required.
        config:
          meta:
            contains_pii: false
      - name: productname
        data_type: string
    description: This table stores roles for a given RoleModule.
