models:
  - name: stg_powerschool__gen
    columns:
      - name: dcid
        data_type: int64
        data_tests:
          - unique:
              config:
                severity: error
        description: Unique identifier for this table. Indexed. Required.
        config:
          meta:
            contains_pii: false
      - name: id
        data_type: int64
        description:
          Sequential number generated by the application, but uniqueness in the
          table is not guaranteed. Indexed.
        config:
          meta:
            contains_pii: false
      - name: cat
        data_type: string
        description:
          "The category the element belongs to. These are the options: Blank,
          wc, per, stdconv, att, logtype, stdconvscale,cit, access, styles,
          groups, feetype, storedsearch, training, printers, ethnicity, hsn,
          coursegroups, dailyatt,activity, subtype, pgfield, consequence,
          teachergroups, nextschools, exitcodes, specprog,
          dailymembtypes,ipadminallow. Indexed."
        config:
          meta:
            contains_pii: false
      - name: name
        data_type: string
        description: The name of the item. Indexed.
        config:
          meta:
            contains_pii: false
      - name: value
        data_type: string
        description: The value of the item stored in an alpha field.
        config:
          meta:
            contains_pii: false
      - name: valuet
        data_type: string
        description: The value of the item stored in a text field.
        config:
          meta:
            contains_pii: false
      - name: value2
        data_type: string
        description: The value of the item stored in an alpha field.
        config:
          meta:
            contains_pii: false
      - name: valuet2
        data_type: string
        description: The value of the item stored in a text field.
        config:
          meta:
            contains_pii: false
      - name: powerlink
        data_type: string
        description: How PowerLink will read this item.
        config:
          meta:
            contains_pii: false
      - name: powerlinkspan
        data_type: string
        description: How PowerLink will read this item in Spanish.
        config:
          meta:
            contains_pii: false
      - name: date_value
        data_type: date
        description:
          The value of the item stored in a date field. For reporting this would
          be the build date.
        config:
          meta:
            contains_pii: false
      - name: date2
        data_type: date
        description:
          The value of the item stored in a date field. For reporting this would
          be the response date.
        config:
          meta:
            contains_pii: false
      - name: log
        data_type: string
        description:
          Log of what this record, such as GPA calculation methods or report
          Builds.
        config:
          meta:
            contains_pii: false
      - name: value_x
        data_type: bytes
        description: The value of the item stored in a BLOB field.
        config:
          meta:
            contains_pii: false
      - name: psguid
        data_type: string
        description:
          If used, a unique identifier for the row. Currently applicable to
          multi-tenant, Schoolnet, and PearsonSuccessNet/Pearson SuccessNet
          Plus.
        config:
          meta:
            contains_pii: false
      - name: valueli
        data_type: int64
        description: The value of the item stored in a long integer field.
        config:
          meta:
            contains_pii: false
      - name: valueli2
        data_type: int64
        description: The value of the item stored in a long integer field.
        config:
          meta:
            contains_pii: false
      - name: valuer
        data_type: float64
        description: The value of the item stored in a real number field.
        config:
          meta:
            contains_pii: false
      - name: sortorder
        data_type: int64
        description:
          For multi item lists where should this one be displayed. Sorted
          numerically lowest to highest.
        config:
          meta:
            contains_pii: false
      - name: schoolid
        data_type: int64
        description: The School_Number of the associated Schools record.
        config:
          meta:
            contains_pii: false
      - name: valueli3
        data_type: int64
        description: The value of the item stored in a long integer field.
        config:
          meta:
            contains_pii: false
      - name: valuer2
        data_type: float64
        description: The value of the item stored in a real number field.
        config:
          meta:
            contains_pii: false
      - name: time1
        data_type: int64
        description:
          The value of the item stored in a date field. For reporting this would
          be the build time.
        config:
          meta:
            contains_pii: false
      - name: time2
        data_type: int64
        description:
          The value of the item stored in a date field. For reporting this would
          be the response time.
        config:
          meta:
            contains_pii: false
      - name: spedindicator
        data_type: int64
        description:
          Used to indicate for reporting if you need SPED kids. True=Yes SPED
          Kids False=No SPED kids.
        config:
          meta:
            contains_pii: false
      - name: valueli4
        data_type: int64
        description:
          The value of the item stored in a long integer field. Currently this
          is used for Excused Attendance code for aparticular school.
        config:
          meta:
            contains_pii: false
      - name: yearid
        data_type: int64
        description:
          A number representing which year the term belongs to, such as 13 for
          2003-2004. The number is equal to theID of the year term divided by
          100. Maintained in the Terms table. Indexed.
        config:
          meta:
            contains_pii: false
