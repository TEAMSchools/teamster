models:
  - name: stg_powerschool__gradecalcschoolassoc
    columns:
      - name: whocreated
        data_type: string
        description:
          The user login that created the record. Defaults to database user.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whencreated
        data_type: timestamp
        description:
          Date and time the record was created. Defaults to system date.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whomodified
        data_type: string
        description:
          The user login that modified the record. Defaults to database user.
          Required.
        config:
          meta:
            contains_pii: false
      - name: whenmodified
        data_type: timestamp
        description:
          Date and time the record was modified. Defaults to system date.
          Required.
        config:
          meta:
            contains_pii: false
      - name: gradecalcschoolassocid
        data_type: int64
        data_tests:
          - unique:
              config:
                severity: error
        description:
          Primary key. Unique sequential number generated by the application.
          Required. Indexed.
        config:
          meta:
            contains_pii: false
      - name: gradecalculationtypeid
        data_type: int64
        description:
          Foreign key. Internal number for the associated GradeCalculationType
          GradeCalculationTypeIdrecord. Required. Indexed.
        config:
          meta:
            contains_pii: false
      - name: schoolsdcid
        data_type: int64
        description:
          Foreign key. Internal number for the associated Schools DCID record.
          Required. Indexed.
        config:
          meta:
            contains_pii: false
    description:
      This table associates GradeCalculationType records to Schools records.
