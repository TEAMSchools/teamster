models:
  - name: stg_deanslist__students
    columns:
      - name: student_id
        data_type: string
        data_tests:
          - unique:
              config:
                store_failures: true
      - name: school_id
        data_type: int64
      - name: student_school_id
        data_type: int64
      - name: custom_fields
        data_type: |
          array<struct<CustomFieldID string, FieldCategory string, FieldKey string, FieldName string, FieldType string, InputHTML string, InputName string, IsFrontEnd string, IsRequired string, LabelHTML string, MinUserLevel string, `Options` string, SourceID string, SourceType string, StringValue string, Value string, SelectedOptions array<string>, NumValue struct<double_value float64, long_value int64>>>
