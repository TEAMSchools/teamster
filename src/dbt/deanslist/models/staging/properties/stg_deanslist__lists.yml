models:
  - name: stg_deanslist__lists
    config:
      enabled: false
      contract:
        enforced: false
    columns:
      - name: list_id
        data_type: int64
        data_tests:
          - unique:
              config:
                store_failures: true
      - name: is_dated
        data_type: boolean
      - name: list_name
        data_type: string
